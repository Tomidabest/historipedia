import React from "react";
import ReactDOM from "react-dom/client";
import App from "./app"; // Changed from "./App" to "./app" to match the renamed file

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);